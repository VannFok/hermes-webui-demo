# Hermes WebUI 需求整理

更新时间：2026-05-31

## 1. 项目定位

这是 Hermes 团队的本地 WebUI 控制台，用来把 4 个 Hermes agent 的状态、消息、输入和 Telegram/WSL Hermes 联动集中到一个更直观的界面里。

目标不是做营销页，而是做一个可以长期打开、实时接收消息、快速给 agent 下达任务的高级感工作台。

## 2. Agent 配置

固定展示 4 个 agent：

- Agent 01
- Agent 02
- Agent 03
- Agent 04

每个 agent 都需要：

- 顶部栏独立入口。
- 使用对应头像。
- 点击 agent 名称或头像可以打开输入框。
- 按空格键可以切换到下一个 agent。
- typing / 正在回答状态要有可视化提示。

头像来源：

- Agent 01：用户提供的头像。
- Agent 02：用户提供的头像。
- Agent 03：用户提供的头像。
- Agent 04：用户提供的头像。

左上角 Hermes Team logo 使用用户提供原版小女孩形象改成黑白配色后的头像，不使用随意生成的替代图。

## 3. 背景与视觉风格

当前背景使用项目里的 `456.jpg`。

背景需求：

- 背景保持干净，不再使用失败的 LightPillar 动态方案作为主视觉。
- 背景图中心绿色环形灯带可以有轻微呼吸灯效果，但不能夸张。
- 背景图已有电路线需要有轻微电流脉冲效果。
- 电流效果要慢一点、痕迹长一点、不卡顿、审美更高级。

曾尝试但放弃的方向：

- `123.png` 背景。
- React Bits / Three.js / LightPillar 动态背景。
- 过亮、过夸张的中心呼吸灯。

## 4. 顶部栏 HUD

顶部栏需要与 `456.jpg` 背景风格一致：

- 黑绿电路感。
- 玻璃金属 HUD 风格。
- 更未来、更高级。
- 不要厚重、不要挡视线。
- 动效和 blur 要轻，避免卡顿。

顶部栏内容：

- 左侧：Hermes Team 标识和 logo。
- 中间/右侧：4 个 agent 的入口。
- 删除右上角实时内存检测。
- 删除原先的 LIVE SIM、模式切换、生成消息等 demo 控件。
- 始终使用流程模式。

agent typing 状态：

- 点击 agent 时只闪一下，不要持续闪。
- 真正 typing / 回答时，可以有围绕 agent 名字椭圆边框的流动灯线。
- 效果不要太夸张，颜色接近头像颜色。

## 5. 输入框

触发方式：

- 点击顶部栏对应 agent。
- 按空格键打开或切换到下一个 agent。
- 点击中间气泡消息后也可以打开输入框。

输入框要求：

- 风格要匹配背景和顶部 HUD。
- 未来设计风。
- 输入框打开时，底部抽屉不能露在后面。
- 点击空白处可以取消输入框。
- 输入框取消后，不应误触导致消息丢失。

发送方式：

- Enter 发送。
- Shift + Enter 换行。

## 6. 消息气泡

普通 agent 消息：

- 没有消息时页面应保持干净。
- 有消息来时，气泡才弹到中间。
- 气泡弹出位置需要比屏幕正中略微偏上一点。
- 气泡文本使用流体式快速输出。
- 消息太长时，气泡内部应可滚动，鼠标滚轮可以查看下方文字。
- 新消息到达要播放提示音。
- 点击气泡可以打开输入框。
- 点击空白处：
  - 如果输入框打开，先关闭输入框。
  - 如果只有气泡展开，再点击空白处让气泡退下。

空格切换 agent 时：

- 如果当前正在查看某条消息，当前消息要自动回收起来。
- 回收后再打开下一个 agent 输入框。

## 7. 抽屉与历史消息

刷新页面后，需要看到每个 agent 最近 3 条普通消息，方便回忆任务进度。

历史来源：

- 由 WebUI 服务端保存到 `hermes-webui-history.json`。
- 不使用 localStorage。
- 普通 agent 消息进入历史。
- Telegram 同步来的普通消息进入历史。
- cron 消息不进入普通抽屉历史。

当前抽屉要求：

- 每个 agent 显示最近 3 条。
- 最新消息排在最前面。
- 不要前后叠放的抽屉样式。
- 点击历史消息时，应该把该消息弹到中间作为气泡查看。
- 不要因为点击旧历史消息直接打开输入框。
- 若需要输入，用户再点击中间气泡打开输入框。

曾经尝试但已取消：

- 抽屉历史消息前后立体叠放。
- 点击旧历史消息直接进入 reply 输入。
- 输入框背后仍露出抽屉内容。

## 8. Reply 引用语义

曾提出的 Telegram reply 类需求：

- 点击最新消息：普通任务输入，不引用。
- 点击第 2/3 条旧消息：可以带引用上下文发送。
- outbox 需要支持：
  - `messageId`
  - `replyToText`
  - `replyToTimestamp`

当前交互调整后：

- 抽屉点击历史消息先弹到中间查看。
- 是否引用该消息，应通过点击中间气泡后的输入流程来承接，而不是抽屉直接打开输入框。

## 9. Telegram 与 Hermes 同步

目标：

- Telegram 里发给某个 agent 的消息和回复，要能同步显示到 WebUI。
- 例如 Telegram 给某个 agent 发消息，WebUI 里对应 agent 应该同步收到信息。
- 切换到其他浏览器标签页时，WebUI 仍要实时收到消息。
- 浏览器标签标题要能闪烁显示发消息来的 agent 名字。

实现方向：

- WebUI 服务提供 `/api/telegram`。
- inbox fallback 使用 `hermes-webui-inbox.ndjson`。
- WSL 或其他外部进程可以把 JSON line 写入 inbox，由 WebUI relay 到浏览器。

注意事项：

- `telegram inbox hello` 是测试消息，不是真实 Hermes 回复。
- 测试消息不应该污染真实 `hermes-webui-history.json`。

## 10. WSL Hermes 接入

真实 Hermes 团队运行在 WSL 中。

WebUI 需要：

- 支持从 Windows WebUI 调用 WSL Hermes。
- 支持 4 个 agent 分别绑定对应 profile。
- 用户不希望每次使用都开多个终端。
- `start-hermes-webui.cmd` 应作为主要启动方式。
- 曾要求每次打开 WSL 时自动启动 `start-hermes-webui.cmd`。

已形成的启动期望：

- 打开 WebUI 使用 `http://127.0.0.1:4173`。
- 启动窗口保持打开时 WebUI 才能使用。
- 后续可继续优化为更自动化的一键启动/自启动。

## 11. Cron 任务消息

cron 任务消息不应作为普通中间气泡弹出。

期望效果：

- cron 消息从对应 agent 头像附近放大到右上角区域。
- 显示为右上角 toast 气泡。
- 3 分钟后自动朝 agent 头像方向缩小消失。
- cron 消息不进入普通抽屉历史，避免和任务对话混在一起。

## 12. 性能要求

整体响应要更快、更轻：

- 打开速度要优化。
- 动效不要卡顿。
- 降低过重的 `backdrop-filter` 和大阴影。
- 减少多层动画同时运行。
- 页面空闲时保持干净，尽量少跑动画。
- 背景电流脉冲要轻量，不要过度消耗性能。

## 13. 音效

需要消息到达音效：

- 普通气泡消息弹出时播放。
- 点击抽屉/历史消息重新弹出气泡时也应有音效。
- 浏览器音频需要用户交互解锁后再播放。

## 14. 当前待清理/待确认事项

- 清理 `hermes-webui-history.json` 中的测试消息 `telegram inbox hello`。
- 修改 inbox 测试，确保以后不会再写入真实历史文件。
- 继续确认 Telegram 真实消息同步链路是否稳定。
- 若后续继续做 reply 引用，需要明确“点击中间气泡输入”时是否默认引用当前气泡，还是只作为普通任务。
